<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pract13" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOnPrimary">@color/my_light_primary_dark</item>
        <item name="android:background">@color/my_light_background</item>
        <item name="android:textColor">@color/my_light_primary_dark</item>
        <item name="colorOnBackground">@color/my_light_primary_dark</item>

        <!-- Кнопки -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/my_light_primary_dark</item>
        <item name="android:textColor">@color/my_light_primary</item>
    </style>

    <style name="Theme.Pract13" parent="Base.Theme.Pract13" />
</resources>